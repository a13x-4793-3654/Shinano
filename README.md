# Shinano

A Chromium-based desktop browser for Microsoft 365 demo environments.

Shinano is being built to keep tabs from multiple isolated profiles in one window,
using Electron. Tabs in the same profile share browser session storage; different
profiles use separate storage.

Initial implementation is in progress. Microsoft 365 sign-in, MFA, and conditional
access requirements still apply.
